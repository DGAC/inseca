{
    "descr": "Root CA deployment",
    "provides": [],
    "requires": [
        "desktop"
    ],
    "builddata": {
        "ca-certs-dir": {
            "descr": "Directory containing root certificates to deploy, each PEM encoded",
            "type": "str"
        }
    },
    "userdata": {}
}
